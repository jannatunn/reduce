import React from "react";

const styles = {
  position: "absolute",
  color: "#fff",
  width: "100%",
  height: "50px",
  bottom: "0",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  backgroundColor: "#000",
};
const Footer = () => {
  return (
    <div style={styles}>
      <h1>ini adalah footer</h1>
    </div>
  );
};

export default Footer;
